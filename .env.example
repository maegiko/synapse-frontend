# Base URL of the Synapse backend. Defaults to http://localhost:8080 when unset.
# The backend's CORS config currently only allows the dev origin http://localhost:5173.
VITE_API_BASE_URL=http://localhost:8080
